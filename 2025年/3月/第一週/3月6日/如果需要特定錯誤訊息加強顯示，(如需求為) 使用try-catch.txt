如果需要特定錯誤訊息加強顯示，(如需求為) 使用try-catch

依照程式結果使用try-catch(如需顯示具體錯誤)，如無須則exceptionhandler

ex.printstacktrace() 列印log

ex.getmassage() 列印(最上)錯誤訊息或資訊



必要使用try-catch才能解決的錯誤，無法單用exceptionhandler解決的錯誤
!需要立即記錄錯誤並補強的
(重試/回滾(回上一步)/取消(退回))

檔案存取(不存在/權限不足)
意外結束(當機)

即時通訊(網路中斷/封包遺失)
股票交易(連線中斷/交易失敗)
線上連線(請求超時)

金融支付(存取錢/交易)

圖像辨識

自動化系統/監視系統