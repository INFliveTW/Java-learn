# 連接 SQL Server 資料庫
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=weather_db;
spring.datasource.username=weather         # 新的用戶名
spring.datasource.password=1QAZ2WSX3EDc4@   # 新的密碼
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA 設定
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=update  # 更新資料庫結構（如果需要）
